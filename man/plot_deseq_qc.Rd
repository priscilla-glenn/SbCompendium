% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.0.1.plot_deseq_qc.R
\name{plot_deseq_qc}
\alias{plot_deseq_qc}
\title{Plot PCA and heatmaps for a DESeq2 dataset}
\usage{
plot_deseq_qc(
  x,
  intgroup = "condition",
  blind = TRUE,
  cor_method = c("pearson", "kendall", "spearman"),
  heatmap_scale = c("row", "column", "none"),
  show_rownames = FALSE,
  cor_cellheight = 20,
  pca_point_size = 4,
  pca_text_vjust = -0.8,
  print_plots = TRUE
)
}
\arguments{
\item{x}{Output from \code{build_deseq()} or a \code{DESeqDataSet}.}

\item{intgroup}{Character. Single \code{colData} column used to color the PCA and
annotate the expression heatmap. Default \code{"condition"}.}

\item{blind}{Logical. Passed to the DESeq2 variance-stabilizing
transformation. Default \code{TRUE}.}

\item{cor_method}{Character. Correlation method passed to \code{stats::cor()}.
One of \code{"pearson"}, \code{"kendall"}, or \code{"spearman"}.}

\item{heatmap_scale}{Character. Scaling applied to the expression heatmap.
One of \code{"row"}, \code{"column"}, or \code{"none"}. Default \code{"row"}.}

\item{show_rownames}{Logical. Show gene names on the expression heatmap.
Default \code{FALSE}.}

\item{cor_cellheight}{Numeric. Height of each row in the sample-correlation
heatmap. Increase this value when sample labels overlap. Default 20.}

\item{pca_point_size}{Numeric. PCA point size. Default 4.}

\item{pca_text_vjust}{Numeric. Vertical adjustment for PCA sample labels.
Default -0.8.}

\item{print_plots}{Logical. If \code{TRUE} (default), prints the PCA and both
heatmaps. Set to \code{FALSE} to create and return them without printing.}
}
\value{
A list containing:
\itemize{
\item \code{vsd}: the variance-stabilized \code{DESeqTransform} object
\item \code{pca_plot}: labeled ggplot2 PCA plot
\item \code{pca_plot_points}: ggplot2 PCA plot with points only
\item \code{pca_data}: data used for the PCA plot
\item \code{percent_var}: percent variance explained by PC1 and PC2
\item \code{cor_matrix}: sample-to-sample correlation matrix
\item \code{correlation_heatmap}: pheatmap object for \code{cor_matrix}
\item \code{expression_heatmap}: pheatmap object for the scaled VST assay
\item \code{annotation_col}: sample annotation used by the expression heatmap
}
}
\description{
Applies DESeq2's variance-stabilizing transformation (VST), creates a
labeled and points-only PCA plots, calculates a sample-to-sample correlation
matrix, and draws correlation and scaled-expression heatmaps. This function is intended
for quality control immediately after \code{build_deseq()}.
}
\examples{
\donttest{
data(example_data)
gene_expr <- importTable(example_data[["nodal_buds_28"]])
dds_out <- build_deseq(gene_expr, TPM_thresh = 5, run_deseq = TRUE)

qc <- plot_deseq_qc(dds_out, print_plots = FALSE)
qc$pca_plot
qc$pca_plot_points
qc$cor_matrix
}
}
