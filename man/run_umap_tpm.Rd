% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2.run_UMAP.R
\name{run_umap_tpm}
\alias{run_umap_tpm}
\title{Run UMAP on compendium TPM expression (auto-infers groups + replicates)}
\usage{
run_umap_tpm(
  df,
  GeneIDV3_col = "GeneIDV3",
  tpm_cols = NULL,
  aggregate_fun = sum,
  TPM_thresh = NULL,
  scale_data = TRUE,
  n_top_genes = 1000,
  seed = NULL,
  n_neighbors = 15,
  min_dist = 0.1,
  metric = "euclidean",
  make_labels = TRUE,
  return_input = FALSE
)
}
\arguments{
\item{df}{A data.frame containing gene IDs (column or rownames) and replicate-level \verb{_TPM} columns.}

\item{GeneIDV3_col}{Character. Name of gene ID column if present. Default "GeneIDV3".}

\item{tpm_cols}{Optional character vector of \verb{_TPM} column names. If NULL, auto-detects.}

\item{aggregate_fun}{Function used to aggregate to GeneIDV3. Default \code{sum}.}

\item{TPM_thresh}{Optional numeric. If not NULL, keeps genes with max TPM > TPM_thresh
prior to scaling/MAD selection.}

\item{scale_data}{Logical. If TRUE (default), scales (z-scores) each gene across samples.}

\item{n_top_genes}{Integer. Number of top MAD genes to use. Default 1000.}

\item{seed}{Optional integer seed for reproducibility (user-picked).}

\item{n_neighbors}{Integer. UMAP n_neighbors. Default 15.}

\item{min_dist}{Numeric. UMAP min_dist. Default 0.1.}

\item{metric}{Character. UMAP distance metric. Default "euclidean".}

\item{make_labels}{Logical. If TRUE (default), label = \code{"{group}.r{rep}"}.}

\item{return_input}{Logical. If TRUE, includes the aggregated TPM matrix in output. Default FALSE.}
}
\value{
A list with:
\itemize{
\item umap: the object returned by \code{umap::umap()}
\item umap_df: data.frame with UMAP1, UMAP2, sample, group, label
\item scaled_data_top: gene x sample matrix used for UMAP (after scaling + MAD filter)
\item tpm_cols: TPM columns used
\item sample_table: mapping of sample columns to group/rep
\item groups: factor levels of inferred groups
\item rep_counts: table of replicate counts per group
\item meta: list of parameters used (including seed, n_neighbors, min_dist)
\item input_matrix: (optional) aggregated TPM matrix (genes x samples)
}
}
\description{
Builds a gene-by-sample matrix from replicate-level \verb{_TPM} columns (ignores \verb{_TPMmean}),
aggregates rows by GeneIDV3, optionally filters by TPM threshold, scales the data,
selects the top variable genes by MAD, then runs UMAP on samples.
}
\details{
Gene identifiers may be stored either in a \code{GeneIDV3} column (default) OR as row names.
}
