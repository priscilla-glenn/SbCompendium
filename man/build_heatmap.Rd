% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/5.0.heatmap_functions.R
\name{build_heatmap}
\alias{build_heatmap}
\title{Build a heatmap export table with TPMmean expression, optional clusters,
and optional FC + annotations}
\usage{
build_heatmap(
  gene_expr,
  clusters = NULL,
  top_de = NULL,
  annot_df = NULL,
  GeneIDV3_col = "GeneIDV3",
  fc_cols = NULL,
  annot_cols = NULL,
  tpmmean_cols = NULL,
  groups = NULL,
  cluster_col_name = "cluster",
  sort_by_cluster = TRUE,
  secondary_sort = c("GeneIDV3", "abs_log2FoldChange")
)
}
\arguments{
\item{gene_expr}{data.frame/matrix with GeneIDV3s as rownames and TPMmean columns.}

\item{clusters}{Optional. Output from run_kmeans(), a data.frame with GeneIDV3 + cluster,
or a named vector of cluster assignments. If NULL, cluster column is omitted.}

\item{top_de}{Optional data.frame containing GeneIDV3 and FC/DE columns.}

\item{annot_df}{Optional data.frame containing GeneIDV3 and annotation columns.}

\item{GeneIDV3_col}{Column name of gene IDs in \code{top_de} / \code{annot_df}. Default "GeneIDV3".}

\item{fc_cols}{Optional vector of FC columns to include from \code{top_de}.
If NULL, uses common DESeq2 columns that exist.}

\item{annot_cols}{Optional vector of annotation columns to include from \code{annot_df}.
If NULL, includes all columns except GeneIDV3_col.}

\item{tpmmean_cols}{Optional TPMmean columns. If NULL uses \code{.get_tpmmean_cols(gene_expr)}.}

\item{groups}{Optional character vector length 2: c(groupA, groupB).
If provided, restricts TPMmean columns to those whose column names contain
either groupA or groupB (fixed substring match).}

\item{cluster_col_name}{Name of cluster column (if clusters supplied). Default "cluster".}

\item{sort_by_cluster}{Logical. If TRUE (default) and clusters provided, sorts by cluster.}

\item{secondary_sort}{Character. One of:
\itemize{
\item "GeneIDV3"
\item "abs_log2FoldChange" (only if log2FoldChange exists)
\item any existing column name in the output
}}
}
\value{
data.frame with GeneIDV3, optional cluster, optional FC, optional annotations,
TPMmean columns (all or group-filtered).
}
\description{
Merges TPMmean expression data (from \code{gene_expr}, GeneIDV3 in rownames),
optional k-means cluster assignments, and optionally FC/DE columns and
annotation columns into a single table suitable for heatmaps and exporting.
}
\details{
You can export either:
\itemize{
\item all TPMmean columns (default), OR
\item only the TPMmean columns corresponding to two DESeq2 group names
(pass \code{groups = c(groupA, groupB)}).
}
}
\examples{
# gene_expr: GeneIDV3s in rownames + TPMmean columns
gene_expr <- data.frame(
  INT1_TPMmean = c(10, 0, 5, 2),
  INT2_TPMmean = c(12, 1, 4, 8),
  INT3_TPMmean = c(3,  8, 6, 2)
)
rownames(gene_expr) <- c(
  "Sobic.001G000100",
  "Sobic.001G000200",
  "Sobic.001G000300",
  "Sobic.001G000400"
)

# 1) Expression-only heatmap table
hm1 <- build_heatmap(gene_expr)
hm1

# Differential expression table (optional)
top_de <- data.frame(
  GeneIDV3 = c(
    "Sobic.001G000100",
    "Sobic.001G000200",
    "Sobic.001G000300"
  ),
  log2FoldChange = c(2.0, -1.2, 3.1),
  padj = c(0.01, 0.20, 0.03),
  stringsAsFactors = FALSE
)

# 2) FC + expression
hm_fc <- build_heatmap(
  gene_expr,
  top_de = top_de
)
hm_fc

# Cluster assignments
clusters_df <- data.frame(
  GeneIDV3 = c(
    "Sobic.001G000100",
    "Sobic.001G000200",
    "Sobic.001G000300"
  ),
  cluster = c(1, 1, 2),
  stringsAsFactors = FALSE
)

# 3) Cluster + expression
hm_cluster <- build_heatmap(
  gene_expr,
  clusters = clusters_df
)
hm_cluster

# Annotation table (optional)
annot_df <- data.frame(
  GeneIDV3 = c(
    "Sobic.001G000100",
    "Sobic.001G000200",
    "Sobic.001G000300",
    "Sobic.001G000400"
  ),
  NAME = c("GeneA","GeneB","GeneC","GeneD"),
  Functional_category = c("TF","Metabolism","Transport","Unknown"),
  stringsAsFactors = FALSE
)

# 4) FC + cluster + annotations + expression
hm_full <- build_heatmap(
  gene_expr,
  clusters = clusters_df,
  top_de = top_de,
  annot_df = annot_df,
  fc_cols = c("log2FoldChange", "padj"),
  annot_cols = c("NAME", "Functional_category"),
  sort_by_cluster = TRUE,
  secondary_sort = "abs_log2FoldChange"
)
hm_full

# 5) Restrict TPMmean columns to two groups (substring match)
hm_groups <- build_heatmap(
  gene_expr,
  top_de = top_de,
  groups = c("INT1", "INT2")
)
hm_groups

# 6) Clusters can also be provided as a named vector
clusters_vec <- c(1, 2, 2)
names(clusters_vec) <- c(
  "Sobic.001G000100",
  "Sobic.001G000200",
  "Sobic.001G000300"
)

hm_vec <- build_heatmap(
  gene_expr,
  clusters = clusters_vec
)
hm_vec

}
