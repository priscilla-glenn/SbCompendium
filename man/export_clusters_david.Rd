% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/6.0.export_clusters_DAVID.R
\name{export_clusters_david}
\alias{export_clusters_david}
\title{Export k-means clusters as DAVID-ready gene lists}
\usage{
export_clusters_david(
  df,
  clusters = NULL,
  out_dir = ".",
  gene_col = "GeneIDV3",
  cluster_col = "cluster",
  prefix = "KMeans_cluster",
  quiet = FALSE
)
}
\arguments{
\item{df}{A data.frame containing at least \code{GeneIDV3} and \code{cluster} columns
(e.g., output from \code{build_heatmap_cluster_export()}).}

\item{clusters}{Integer vector of cluster numbers to export. If NULL (default),
exports all unique clusters found in \code{df$cluster}.}

\item{out_dir}{Output directory for the text files.}

\item{gene_col}{Name of the Gene ID column. Default "GeneIDV3".}

\item{cluster_col}{Name of the cluster column. Default "cluster".}

\item{prefix}{Filename prefix. Default "KMeans_cluster".}

\item{quiet}{Logical. If FALSE (default), prints files written.}
}
\value{
Invisibly returns a character vector of file paths written.
}
\description{
Exports one text file per cluster containing gene IDs converted from
\verb{Sobic.*} format to DAVID-compatible \verb{SORBI_3*} format (v3 naming),
suitable for GO/annotation tools like DAVID.
}
\details{
Files are written as one gene ID per line.
}
\examples{
# Example cluster table (similar to build_heatmap() output)
df <- data.frame(
  GeneIDV3 = c(
    "Sobic.001G000100",
    "Sobic.001G000200",
    "Sobic.001G000300",
    "Sobic.001G000400"
  ),
  cluster = c(1, 1, 2, 3),
  stringsAsFactors = FALSE
)

# Write DAVID-ready gene lists to a temporary directory
out_dir <- tempdir()

files_written <- export_clusters_david(
  df,
  out_dir = out_dir
)

files_written

# Export only selected clusters
files_written2 <- export_clusters_david(
  df,
  clusters = c(1, 2),
  out_dir = out_dir,
  quiet = TRUE
)

files_written2

}
