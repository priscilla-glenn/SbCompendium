% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.1.run_contrast.R
\name{run_contrast}
\alias{run_contrast}
\title{Run a DESeq2 contrast and return results + filtered top DE table}
\usage{
run_contrast(dds, groupA, groupB, alpha = 0.05, lfc_thresh = 2.32)
}
\arguments{
\item{dds}{A DESeq2::DESeqDataSet with a \code{condition} column in colData.}

\item{groupA, groupB}{Condition levels to compare (groupA vs groupB).}

\item{alpha}{Adjusted p-value cutoff.}

\item{lfc_thresh}{Absolute log2FC cutoff.
column in \code{colData(dds)} used for contrasts.}
}
\value{
A list with:
\itemize{
\item res: DESeqResults
\item res_df: data.frame with GeneIDV3 column
\item top_de: filtered data.frame
}
}
\description{
Run a DESeq2 contrast and return results + filtered top DE table
}
\examples{
# Create a small count matrix
counts <- matrix(
  c(100,120,30,40,
    20,15,90,85,
    80,75,40,50),
  nrow = 3,
  byrow = TRUE
)

rownames(counts) <- c(
  "Sobic.001G000100",
  "Sobic.001G000200",
  "Sobic.001G000300"
)

colnames(counts) <- c(
  "INT1_rep1",
  "INT1_rep2",
  "INT2_rep1",
  "INT2_rep2"
)

# Sample metadata
colData <- S4Vectors::DataFrame(
  condition = factor(c("INT1","INT1","INT2","INT2"))
)

# Build DESeqDataSet
dds <- DESeq2::DESeqDataSetFromMatrix(
  countData = counts,
  colData = colData,
  design = ~ condition
)

# Run DESeq quickly for the example
dds <- DESeq2::DESeq(dds, quiet = TRUE)

# Run contrast (INT2 vs INT1) with alpha = 0.05
# Only genes with adjusted p-value < 0.05 and |log2FC| > 2.32 are returned in top_de
res <- run_contrast(dds, "INT2", "INT1", alpha = 0.05, lfc_thresh = 2.32)

# Inspect filtered DE genes
res$top_de

}
