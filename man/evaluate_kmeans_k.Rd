% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4.0.Kmean_functions.R
\name{evaluate_kmeans_k}
\alias{evaluate_kmeans_k}
\title{Evaluate optimal k for k-means clustering (prints plots)}
\usage{
evaluate_kmeans_k(
  mat,
  k_max = 20,
  dedup = TRUE,
  nstart = 50,
  iter_max = 1000,
  seed = NULL,
  algorithm = "Lloyd",
  print_plots = TRUE
)
}
\arguments{
\item{mat}{Numeric matrix with genes as rows and samples as columns.}

\item{k_max}{Integer. Maximum number of clusters to evaluate. Default 20.}

\item{dedup}{Logical. If TRUE (default), remove duplicate rows for k-evaluation only.}

\item{nstart}{Integer. Passed to kmeans during evaluation. Default 50.}

\item{iter_max}{Integer. Passed to kmeans during evaluation. Default 1000.}

\item{seed}{Optional integer seed for reproducibility.}

\item{algorithm}{Character. kmeans algorithm ("Hartigan-Wong", "Lloyd", "Forgy", "MacQueen").
Default "Lloyd" for stability.}

\item{print_plots}{Logical. If TRUE (default), prints the plots immediately.}
}
\value{
Invisibly returns a list with silhouette_plot, wss_plot, and meta.
}
\description{
Generates and prints diagnostic plots to help determine the optimal number
of clusters (k) using the silhouette and WSS methods.
}
\examples{
# Small numeric matrix (genes x samples)
mat <- matrix(
  c(1,2,3,
    2,1,3,
    9,8,7,
    8,9,7,
    4,4,4),
  nrow = 5,
  byrow = TRUE
)
rownames(mat) <- paste0("gene", 1:5)
colnames(mat) <- c("A_TPMmean", "B_TPMmean", "C_TPMmean")

if (requireNamespace("factoextra", quietly = TRUE)) {
  out <- evaluate_kmeans_k(mat, k_max = 4, seed = 1, print_plots = FALSE)
  names(out)
} else {
  message("Skipping: 'factoextra' not installed.")
}
}
